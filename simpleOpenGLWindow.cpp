#include <GLUT/GLUT.h>
#include <OpenGL/OpenGL.h>
#include <stdio.h>
int main(int argc, char **argv)
{
      printf("%s", "output something");
      return 0;
      glutInit(&argc, argv);
      return 0;
      return 0;
}
